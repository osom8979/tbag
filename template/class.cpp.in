/**
 * @file   ${SOURCE_FILENAME}
 * @brief  ${SOURCE_BRIEF}
 * @author ${AUTHOR}
 * @date   ${DATE}
 */

#include <${INCLUDE_PAIR}>

// ${NAMESPACE_OPEN_COMMENT}
${NAMESPACE_OPEN}
// ${NAMESPACE_OPEN_COMMENT}

${CLASSNAME}::${CLASSNAME}()
{
    // TODO: Insert default constructor.
}

${CLASSNAME}::${CLASSNAME}(${CLASSNAME} const & obj)
{
    this->copy(obj);
}

${CLASSNAME}::${CLASSNAME}(${CLASSNAME} && obj)
{
    this->swap(obj);
}

${CLASSNAME}::~${CLASSNAME}()
{
    // TODO: Insert destructor.
}

${CLASSNAME} & ${CLASSNAME}::operator =(${CLASSNAME} const & obj)
{
    return this->copy(obj);
}

${CLASSNAME} & ${CLASSNAME}::operator =(${CLASSNAME} && obj)
{
    this->swap(obj);
    return *this;
}

${CLASSNAME} & ${CLASSNAME}::copy(${CLASSNAME} const & obj)
{
    if (this != &obj) {
        // TODO: Insert copy code.
    }
    return *this;
}

void ${CLASSNAME}::swap(${CLASSNAME} & obj)
{
    if (this != &obj) {
        // TODO: Insert swap code.
    }
}

// ${NAMESPACE_CLOSE_COMMENT}
${NAMESPACE_CLOSE}
// ${NAMESPACE_CLOSE_COMMENT}

